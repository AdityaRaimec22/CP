cdef update_admin_email_content_info(admin_email_content):
    """Updates the admin email content object and model. Creates the corresponding
    AdminEmailContentModel if it doesn’t already exist.

       Args:
           admin_email_content: AdminEmailContentInfo. Admin email content info object to
               update.
        """
   def get_admin_email_content_info():
       """Gets the AdminEmailContentModel instance and creates the corresponding  
       AdminEmailContentInfo domain object.

       Returns:
           AdminEmailContentInfo|None. The admin email content info domain object or
           None if the corresponding model doesn’t exist yet."""


